import AppendixA from "./AppendixA"

export default function Appendix() {
  // Keep this component for backward compatibility with /appendix route
  // It now just shows Appendix A
  return <AppendixA />
}
